/* 
    index.html 에서 사용되는 js
*/
function login(username, password) {
    //login 처리
    if(username != null && password != null) {
        // username 값과 password 값을 체크
    }
    return true;
};

function logout(username) {
    // log out 처리    
};